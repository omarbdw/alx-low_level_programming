#include "main.h"
/**
 * main - checks for uppercase character
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */
int _isupper(char c)
{
  char c;
  scanf("", c);
  if ('A' >= c <= 'Z')
    {
      return (1);
    }
  else
    {
  return (0);
    }
    }
